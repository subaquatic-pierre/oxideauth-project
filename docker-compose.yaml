services:
  # oxideauth:
  #   container_name: oxideauth-api
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.dev
  #   env_file: ./oxideauth/.env.dev
  #   user: "1000:1000"
  #   ports:
  #     - "8000:8000"
  #   volumes:
  #     - ./oxideauth:/app/oxideauth
  #     - ./oxideauth-macros:/app/oxideauth-macros
  #   depends_on:
  #     - postgres
  #     - redis
  #   networks:
  #     - oxideauth_net # Use the explicit network name defined below

  postgres:
    container_name: oxideauth-postgres
    image: postgres:16
    environment:
      POSTGRES_DB: dev_db
      POSTGRES_USER: test_user
      POSTGRES_PASSWORD: password
      PGDATA: /var/lib/postgresql/data/pgdata

    ports:
      - "5432:5432"
    volumes:
      - ./data/psql:/var/lib/postgresql/data/pgdata
    restart: always
    networks:
      - oxideauth_net

  redis:
    container_name: oxideauth-redis
    image: redis:7-alpine
    command: redis-server --save 60 1
    ports:
      - "6379:6379"
    volumes:
      - ./data/redis:/data # Store Redis data persistently
    restart: always
    networks:
      - oxideauth_net

# Define the networks section
networks:
  oxideauth_net: # The corrected network definition name
    driver: bridge
