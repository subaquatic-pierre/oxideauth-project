# Store Module

## TODO:

### Filter by tags

- query `filter_by_tags` generic over any `StoreMeta` and `Fields` schema
- store meta trait `has_tags`
- `FilterByTags` trait

### Join tables

- figure how to implement generic queries for joins
- implement join queries for and join tables, ie. `role_permission` or `membership_role` tables
