- SDKs for majow languages and frameworks
- SDK support for in memory caching as well as redis
- SDK support for endpoints receiving push notification updates on full node updates

- implement validator nodes, which are small nodes at the edge which clients interact with to get fresh auth information, instead of requesting auth validating resources from full nodes
